@{
    ViewData["Title"] = "Acesso Negado";
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Acesso Negado - Isodoc</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
            font-family: system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
        }
        .error-card {
            text-align: center;
            padding: 3rem;
            background: white;
            border-radius: 1rem;
            box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
            max-width: 500px;
            width: 90%;
        }
        .icon-danger {
            font-size: 4rem;
            color: #dc3545;
            margin-bottom: 1.5rem;
        }
    </style>
</head>
<body>
    <div class="error-card">
        <i class="bi bi-shield-lock-fill icon-danger"></i>
        <h1 class="h3 mb-3">Acesso Negado</h1>
        <p class="text-muted mb-4">Você não tem permissão para acessar esta página. Se você acredita que isso é um erro, entre em contato com o administrador.</p>
        <div class="d-grid gap-2">
            <a href="/" class="btn btn-primary">Voltar para o Início</a>
            <form asp-action="Logout" asp-controller="Account" method="post" class="d-inline">
                <button type="submit" class="btn btn-outline-secondary w-100">Sair e tentar com outra conta</button>
            </form>
        </div>
    </div>
</body>
</html>
